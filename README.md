# FormCadastro
Testes Automatizados com Playwright

Este repositório contém um projeto com uma página de Cadastro de Usuários e um conjunto de testes automatizados usando Playwright.

📌 Conteúdo do Repositório

Página HTML (index.html)

Estilos (estilo.css)

Script da página (script.js)

Testes automatizados (/tests)

Arquivo de configuração do Playwright

README explicando como rodar o projeto

✔️ Como Instalar

Instale o Node.js

No terminal, dentro da pasta do projeto, execute:

npm install


Instale o Playwright:

npx playwright install

▶️ Como Rodar os Testes

Para executar todos os testes, use:

npx playwright test


Para abrir a interface visual do Playwright:

npx playwright test --ui

📄 Sobre os Testes

Os testes verificam:

Carregamento inicial da página

Validação de campos obrigatórios

Envio do formulário

Inserção de usuário na lista

Limpar cadastros
